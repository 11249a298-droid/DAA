#include <stdio.h>
 int fib(int n)
11. {
 if (n <= 1)
{
 return n;
 }
return fib(n - 1) + fib(n - 2);
}

 int main()
 {
 int n, result = 0;
 printf("Enter the value of n: ");
 scanf("%d", &n);

 
 if (n != 1 || n < 0)
 {
 printf("Invalid input\n");
 return 1;
}

return 0;
}
